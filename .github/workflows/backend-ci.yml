name: Backend CI

on:
  push:
    branches: [main, uat, qa]
  pull_request:
    branches: [main, uat, qa]

jobs:
  build:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v3

      - name: Setup .NET
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: "8.0.x"

      - name: Restore packages
        run: dotnet restore backend/codename-bulldog.sln

      - name: Build solution
        run: dotnet build backend/codename-bulldog.sln --configuration Release --no-restore

      - name: Verify code formatting
        run: dotnet format backend/codename-bulldog.sln --verify-no-changes

      - name: Run tests
        run: dotnet test backend/codename-bulldog.sln --no-build --verbosity normal
